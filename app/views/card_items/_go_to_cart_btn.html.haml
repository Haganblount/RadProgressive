- if @card_items.any? && @card_items.sum(:count) > 0 
  = link_to new_card_path, remote: true, class: 'btn has-cart-items go-to-cart-btn' do
    Go to Cart
    %span.card-items-count (#{@card_items.sum(:count)})
- else
  = link_to new_card_path, remote: true, class: 'btn go-to-cart-btn btn-flag' do
    Go to Cart