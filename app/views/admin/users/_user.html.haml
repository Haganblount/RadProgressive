%tr
  %td= user.email
  %td= user.created_at.to_usa
  %td
    = link_to 'Edit', edit_admin_user_path(user), class: 'btn btn-info'
    = link_to 'Delete', [:admin, user], method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger'